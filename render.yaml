services:
  - type: web
    name: zagonel-kpi
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --timeout 300 --workers 1 --worker-class sync --keep-alive 5
    envVars:
      - key: API_KEY
        value: zagonel2026
      - key: GITHUB_TOKEN
        value: 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
      - key: GITHUB_REPO
        value: BrunoPedrolo/zagonel-kpi
    disk:
      name: data
      mountPath: /opt/render/project/src
      sizeGB: 1
